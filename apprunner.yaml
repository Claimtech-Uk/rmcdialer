version: 1.0
runtime: nodejs20
build:
  commands:
    - cd services/voice-ws
    - npm ci --omit=dev
run:
  command: node services/voice-ws/server.js
  network:
    port: 8080
health:
  path: /healthz
  interval: 10
  timeout: 5
  healthy_threshold: 1
  unhealthy_threshold: 5


